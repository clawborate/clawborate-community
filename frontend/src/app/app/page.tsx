/**
 * Dashboard root page — rendered inside (dashboard)/layout.tsx.
 *
 * The three-column shell in layout.tsx owns all visible content
 * (LeftNav, MainPanel, RightChatPanel). This page is intentionally
 * empty to avoid a nested scroll container.
 */
export default function DashboardPage() {
  return null
}
